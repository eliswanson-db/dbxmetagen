# OPTION 2: BioBERT-Only Approach (SECURITY FOCUSED)  
# Removes GLiNER entirely to avoid compatibility issues

# Base dependencies
numpy>=1.21.5,<2.0
pandas>=1.5.0,<2.1.0

# Modern ML stack - latest compatible versions
torch>=2.1.0,<2.7.0  # Allow range for flexibility
transformers>=4.35.0,<4.50.0  # Allow some flexibility
tokenizers>=0.19.0

# PII detection only (no GLiNER)
presidio-analyzer==2.2.358
presidio-anonymizer==2.2.358

# NLP dependencies
spacy>=3.7.0,<3.9.0

# Additional modern ML support
packaging>=21.0
accelerate>=0.20.0
safetensors>=0.3.0
huggingface-hub>=0.17.0
