# NER and PII/PHI Detection Requirements - COMPATIBILITY FIXED
# This version addresses torch/transformers/gliner conflicts

# Base dependencies - pinned for stability
numpy>=1.21.5,<2.0
pandas>=1.5.0,<2.1.0

# SOLUTION: Use compatible versions that work together
# torch 2.1.0 is stable and widely supported
torch==2.1.0
# transformers 4.35.0 works with torch 2.1.0 and is stable
transformers==4.35.0
# tokenizers compatible with transformers 4.35.0
tokenizers>=0.15.0,<0.20.0

# GLiNER - keep current version (works with torch 2.1.0)
gliner==0.2.5

# PII detection dependencies
presidio-analyzer==2.2.358
presidio-anonymizer==2.2.358

# NLP dependencies
spacy>=3.7.0,<3.9.0

# Additional ML support for better compatibility
packaging>=21.0
accelerate>=0.20.0  # Better GPU support
safetensors>=0.3.0  # Modern model loading