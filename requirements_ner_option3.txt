# OPTION 3: Flexible Ranges (LET PIP RESOLVE)
# Uses version ranges to let pip find compatible versions

# Base dependencies
numpy>=1.21.5,<2.0
pandas>=1.5.0,<2.1.0

# Flexible ML stack - let pip resolve
torch>=2.1.0,!=2.2.0  # Avoid 2.2.0 which has known issues
transformers>=4.30.0,<4.45.0  # Wide range
tokenizers>=0.15.0
gliner>=0.2.0,<0.3.0

# PII detection
presidio-analyzer>=2.2.0,<2.3.0
presidio-anonymizer>=2.2.0,<2.3.0

# NLP dependencies
spacy>=3.7.0,<4.0.0

# Support libraries
packaging>=21.0
accelerate>=0.20.0
safetensors>=0.3.0
